# Audiveris CLI 本地验证结果模板

> 复制此模板记录一次由用户在本地手动完成的验证。不要把未执行或未确认的 CLI 参数
> 标记为已验证。提交记录或 fixture 前，必须移除本地路径和其他敏感信息。

- **Date:**
- **OS:**
- **Audiveris version:**
- **Java version if known:**
- **Audiveris executable path:**
- **Input file path:**
- **Output directory:**
- **Command attempted:**
- **Exit status:**
- **stdout summary:**
- **stderr summary:**
- **Output files generated:**
- **MXL file size:**
- **MusicXML extracted:**
- **Was local path found in output?:**
- **Was output redacted before commit?:**
- **Did dev-only MusicXML API parse it?:**
- **Did `validate:musicxml` pass after fixture update?:**
- **Notes / issues:**

