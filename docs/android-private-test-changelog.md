# Android 本地私测版本变更记录

本文件只记录可追溯的 Android 本地私测候选。调试签名包用于产品所有者私下安装测试，不是应用商店包、专用 release 签名包或正式评分产品。

## 0.2.0（versionCode 2）

构建状态：P102 候选已通过 CI；P103 探索性音高观察切片待 CI。

### 用户可见变化

- P89：四类独立本地练习改为会话内随机题序。同一会话内每道题可复现，并会先走完整题库再循环；关闭应用后重新生成题序，不形成学习历史或难度自适应。
- P90：新增 Android 本机错题复练。查看答案后，答错题加入并置顶，答对题移除，最多保留 12 项；首页可进入复练，并可通过页面内二次确认清空。本机只保存复现题目所需的最小信息，不保存作答、答案、音频、分数、准确率、账号或完整历史。
- P91：下载候选包前增加独立工件复核门禁。CI 会重新计算 APK 摘要与字节数，并交叉核对校验文件、构建报告、版本和 commit；缺失、多余或不一致会阻止上传。这是交付可靠性增强，不改变练习评分边界。
- P93：新增真实挂载移动 React App 的本机复练行为回归，自动覆盖错题加入/移除、清除确认、历史导航和存储失败降级。
- P94：新增离线“本地参考钢琴”独立页，并在四类练习中提供默认收起的找音入口。包含三档 13 键音域、多指、音量、延音、全停和中文失败重试；弹奏不保存、不上传，也不生成成绩。
- P95：本机复练新增稳定题目标识，已有 v1 记录会在本机原地升级为 v2，避免后续题库扩容时旧错题指向另一题；迁移失败提示不阻断练习。本切片未扩充三难度题量，也未改变课程或云端 v1。
- P96：Android 四类听辨练习均增加基础、进阶、挑战三档，每一题型每档至少 20 个版本化组合；手动难度通过音域、半音、跨度、节奏型、速度和旋律跳进区分。共享 Web、课程和云端 v1 不变；该难度仍是开发者编排练习，不冒充教育校准或正式自适应评估。
- P97：新增用户主动开启的本地实时音高反馈，可靠帧显示音名、Hz 与 cents 偏移；安静、低置信度和超范围时不会强行显示音名。停止、清空、切页或进入后台会释放麦克风；不录音、不保存、不上传，也不生成正式评分。
- P98：实时音高页新增 C3–C6 音名网格、最近 5 / 10 / 15 秒滚动时间轴、当前游标和可调目标参考线；不可靠人声与采样间隙会明确断线，曲线只存于内存并可一键停止清空。
- P99：实时音高页新增当前会话录音、停止、回放、停止回放和丢弃；必须再次主动开始，默认不落盘、不上传。回放前停止麦克风监听，切页、后台和卸载会释放录音、媒体流、回放与 object URL。
- P100：新增版本化本地练声目标生成器，包含单音、音程、大调音阶、五声音型和主和弦音型；可配置根音、方向、BPM、八度、循环和参考播放范围。参考音开始时会停止麦克风，避免扬声器串入实时曲线。
- P101：练声目标音块可叠加到实时曲线；目标时段内提供“接近 / 偏高 / 偏低 / 不足以判断”非评分反馈，并可手动选择第一组中的单音片段循环 3 次复练。
- P102：新增用户主动保存的本机练声记录，最多 20 条、每条最近 600 帧、200 字备注和可选 5 MB 录音；支持目标快照、曲线回看、录音回放、单条删除、二次确认全部清除和不含录音二进制的 JSON 导出。
- P103：当前曲线和已保存曲线新增最近片段探索性观察，展示可靠音高分布、持续音短期波动、周期性音高摆动候选和尾部音高走向；数据不足会拒绝判断，不提供分数、声部、颤音质量或医学结论。

### 已知限制与安装提醒

- 当前候选仍使用 Android Debug 调试签名，不是专用 release 签名正式包。
- 尚未完成真实 Android 手机的安装、断网冷启动、扬声器音频、跨重启复练、后台恢复和长循环验收；自动化 PASS 不等于真机 PASS。
- 安装 0.2.0 前需先卸载旧 debug 包，再安装新 APK。卸载会清除旧包的本机复练队列、练声曲线和录音；JSON 导出只保留曲线与摘要，不包含录音。请勿把它当作覆盖升级或数据迁移已验证的证据。
- APK 不申请网络或宽泛存储权限；P97 起仅为用户主动开始的本地实时音高反馈申请麦克风权限。不依赖 Vercel、Supabase 或远程 `server.url`，也不提供账号、云同步或正式评分。

### 构建状态

`versionName 0.2.0` / `versionCode 2` 的 P102 候选已由 run `29504298671` 构建；artifact `8377845031` 内 APK SHA-256 为 `3ac74de46162daead3ec3f4bb74536d998ce880c6af74fea14970a8ee489e64c`，ZIP digest 为 `5f35ed5890f840aefd96d27d92a623a8517eb9c6083620946d2f5580bba034af`。P103 必须等待本切片 CI 生成新工件，不得沿用该摘要。
